# frozen_string_literal: true

class GbifSpeciesList
    def self.download(address:, destination:)
        Helper.download(address: address, destination: destination)
    end

    def self.read(file_name)

    end
end